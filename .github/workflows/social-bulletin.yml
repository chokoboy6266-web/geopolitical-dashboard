name: Scheduled Social Bulletin

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch: {}

jobs:
  bulletin:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger bulletin endpoint
        run: curl -sf https://geopolitical-dashboard-steel.vercel.app/api/bulletin
